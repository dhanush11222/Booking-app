class AppointmentController {
  void bookAppointment(String doctorId, DateTime dateTime) {
    // Save the appointment (e.g., to dummy data or backend API)
    print('Appointment booked with $doctorId at $dateTime');
  }
}
